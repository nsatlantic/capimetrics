import PageBanner from '../../layout/PageBanner'

const BannerSection = () => (
  <PageBanner
    badge="Our Services"
    title="Financial expertise built"
    highlight="for every stage."
    subtitle="From startup to scale — we bring the right skill to the right problem, exactly when you need it."
  />
)

export default BannerSection
